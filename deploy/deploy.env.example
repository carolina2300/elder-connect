# Copy to deploy.env (gitignored) and fill in. Do NOT commit deploy.env.
HOST=your.vps.host.or.ip
SSH_USER=ubuntu
SSH_KEY=~/.ssh/eldercare_vps      # path to private key
SSH_PASSWORD=the-ubuntu-password  # used for SSH login AND sudo
